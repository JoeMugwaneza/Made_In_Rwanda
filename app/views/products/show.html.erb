<div class="container-fluid">
  <h1><%= @product.name %></h1>
  <h2><%= @product.description %></h2>
  <h2> <%= @product.price %></h2>

    <div class="item">
      <img src="<%= @product.images.sample.url %>">
    </div>
    </div>

 <div>
  <%= link_to "Edit", edit_product_path, class: "btn btn-default" %> <%= link_to "Delete", product_path, method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-default" %>
  <br> <br> <%= link_to "Back", root_path, class: "btn btn-default"%> 
   <%= link_to "View More Images", "/images?product_id=#{@product.id}", class: "btn btn-default" %>
 </div>
