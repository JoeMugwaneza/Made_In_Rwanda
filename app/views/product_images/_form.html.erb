<%= form_for @product_image, url: product_product_images_path do |f| %>
  <%= f.label :Upload_image %>
  <%= f.file_field :image_url %>
  <%= f.hidden_field :product_id, {:value => @product.id} %>
  <%= f.submit 'Upload Image' %>
<% end %>